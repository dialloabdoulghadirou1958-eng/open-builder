# ── Mohua API (Optional, if not configured, local mode is used) ──
VITE_MOHUA_API_URL=https://mohua.u14.app

# ── u14.app SSO (Optional, if not configured, SSO login button is hidden) ──
VITE_SSO_URL=https://u14.app
VITE_SSO_CLIENT_ID=open-builder
VITE_SSO_REDIRECT_URI=http://localhost:5173/auth/callback
